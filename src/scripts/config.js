require.config({
    // baseURL is the scripts folder (where this is stored);
    deps:['main'],
    paths: {
        lodash : '../bower_components/lodash/dist/lodash',
        jquery : '../bower_components/jquery/dist/jquery'
    }
});